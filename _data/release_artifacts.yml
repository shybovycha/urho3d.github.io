filter-criterias:
  64-bit: true
  32-bit: true
  STATIC: true
  SHARED: true


platforms:
  Windows:
    icon: fa fa-windows fa-2x
    archs:
      64-bit:
        - STATIC.zip
        - STATIC-D3D.zip
        - SHARED.zip
        - SHARED-D3D.zip
      32-bit:
        - STATIC.zip
        - STATIC-D3D.zip
        - SHARED.zip
        - SHARED-D3D.zip

  OSX:
    icon: fa fa-apple fa-2x
    archs:
      64-bit:
        - STATIC.tar.gz
        - SHARED.tar.gz
      32-bit:
        - STATIC.tar.gz
        - SHARED.tar.gz

  Linux:
    icon: fa fa-linux fa-2x
    archs:
      64-bit:
        - STATIC.tar.gz
        - STATIC.rpm
        - STATIC.deb
        - SHARED.tar.gz
        - SHARED.rpm
        - SHARED.deb
      32-bit:
        - STATIC.tar.gz
        - STATIC.rpm
        - STATIC.deb
        - SHARED.tar.gz
        - SHARED.rpm
        - SHARED.deb

  iOS:
    icon: icon-mobile-device fm-2x
    archs:
      64-bit:
        - STATIC.tar.gz
      32-bit:
        - STATIC.tar.gz

  Android:
    icon: fa fa-android fa-2x
    archs:
      64-bit:
        - STATIC.tar.gz
        - STATIC-IA.tar.gz
        - SHARED.tar.gz
        - SHARED-IA.tar.gz
      32-bit:
        - STATIC.tar.gz
        - STATIC-IA.tar.gz
        - SHARED.tar.gz
        - SHARED-IA.tar.gz

  Raspberry-Pi:
    icon: icon-raspberrypi fm-2x
    archs:
      32-bit:
        - STATIC.tar.gz
        - STATIC.rpm
        - STATIC.deb
        - SHARED.tar.gz
        - SHARED.rpm
        - SHARED.deb
